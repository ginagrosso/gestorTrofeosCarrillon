# Commit Templates

## Ejemplos por tipo

```
add(articulos): agregar endpoint de actualización masiva de precios

add(react-fsd): crear feature ArticuloForm con React Hook Form y Zod

fix(auth): corregir validación de token expirado en middleware

update(clientes): agregar campo celular al schema Zod

deleted(proveedores): eliminar campo rubro_legacy sin uso

refactor(articulos): separar lógica de cálculo de costo a service

docs(cloud-functions-mvc): agregar ejemplos de repository en skill

add(ventas): agregar módulo completo ventas (schema, repo, service, controller, routes)
```
